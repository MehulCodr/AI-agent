# Magent Agent Instructions

- Keep the code simple, readable, and CLI-first.
- Prefer the Go standard library unless a dependency clearly earns its place.
- Do not add TUI behavior for v1.
- Keep user approval boundaries explicit for edits and shell commands.
- Implement MVP features incrementally; avoid speculative abstractions.
